[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "persisthawk"
version = "0.1.0"
description = "Linux Persistence & LOLBin Hunter"
requires-python = ">=3.11"

[project.scripts]
persisthawk = "app.main:main"
